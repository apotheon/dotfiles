# dotfiles

The root directory of the repository contains this README and any directories
and files found in a user's home directory.

The `config` directory contains subdirectories that are normally placed in the
`.config` directory in accordance with XDG Base Directory Standard config file
management.
